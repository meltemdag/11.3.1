{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "history-app",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os, http.server; http.server.test(HandlerClass=http.server.SimpleHTTPRequestHandler, port=int(os.environ.get('PORT', 8080)))"],
      "port": 8080,
      "autoPort": true
    }
  ]
}
